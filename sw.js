const CACHE='ticket-scanner-v5-1';self.addEventListener('install',e=>self.skipWaiting());self.addEventListener('activate',e=>e.waitUntil(self.clients.claim()));self.addEventListener('fetch',e=>{if(e.request.method!=='GET')return;e.respondWith(caches.open(CACHE).then(async c=>{const hit=await c.match(e.request);if(hit)return hit;try{const r=await fetch(e.request);if(new URL(e.request.url).origin===location.origin)c.put(e.request,r.clone());return r}catch(err){return caches.match('./index.html')}}))});